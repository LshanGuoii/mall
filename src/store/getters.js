export default{
    cartLength(state){
        return state.cartList.length
    },
    cartList(state){
        return state.cartList
    }
      
}

// const getters = {
//     cartList(state) {
//       return state.cartList
//     },
//     cartLength(state) {
//       return state.cartList.length
//     }
//   }
  
//   export default getters
  